{
  "name": "provisioner",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "tools/provisioner/src",
  "targets": {
    "run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ts-node tools/provisioner/src/main.ts",
        "cwd": "tools/provisioner"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["tools/provisioner/**/*.ts"]
      }
    }
  },
  "tags": ["type:tool", "layer:ops"]
}
