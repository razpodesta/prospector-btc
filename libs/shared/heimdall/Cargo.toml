[package]
name = "prospector-shared-heimdall"
version = "0.1.0"
edition = "2021"

[dependencies]
# El estándar de oro para logging en Rust
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
